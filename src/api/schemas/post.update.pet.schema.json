{
    "title": "Update pet with POST",
    "description": "Updating pet with POST request",
    "type": "object",

    "properties": {
        "code": {
            "$id": "#/properties/code",
            "description": "response status code",
            "type": "integer"
        },
        "type": {
            "$id": "#/properties/type",
            "description": "unknown",
            "type": "string"
        },
        "message": {
            "$id": "#/properties/message",
            "description": "Contains updating pet's id",
            "type": "string"
        }
    },
    "required": [
        "code", 
        "message"
    ] 
}